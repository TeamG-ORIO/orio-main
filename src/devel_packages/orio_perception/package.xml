<?xml version="1.0"?>
<package format="2">
  <name>orio_perception</name>
  <version>0.1.0</version>
  <description>
    ORIO perception: grasp-pose generation and label-location services from the
    ZED + Xtion cameras (GroundingDINO + SAM + Open3D + a grasp solver). Runs in a
    host Python venv (heavy ML deps), not the ROS docker image.
  </description>
  <maintainer email="student@todo.todo">ORIO Team G</maintainer>
  <license>TODO</license>

  <buildtool_depend>catkin</buildtool_depend>

  <exec_depend>rospy</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>std_msgs</exec_depend>
  <exec_depend>std_srvs</exec_depend>

  <export>
  </export>
</package>
